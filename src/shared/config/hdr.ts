/** 色彩增益滑杆最小 EV 值；0 表示关闭 HDR 增益 */
export const FLASH_STOPS_MIN = 0
/** 色彩增益滑杆最大 EV 值；5 EV 已接近强刺激上限 */
export const FLASH_STOPS_MAX = 5
/** 色彩增益滑杆步进，保证微调时不会过粗 */
export const FLASH_STOPS_STEP = 0.05
/** 色彩增益默认值；默认关闭 HDR */
export const DEFAULT_FLASH_STOPS = 0
/** 点击开启 HDR 时采用的推荐 EV 值 */
export const ENABLED_FLASH_STOPS = 1
